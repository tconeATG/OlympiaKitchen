<footer class="footer">
  <small>
  Olympian Kitchen
  </small>
  <nav>
    <ul>
      <li><%= link_to "About",   about_path %></li>
      <li><%= link_to "Contact", contact_path %></li>
      <li><a href="#">News</a></li>
    </ul>
  </nav>
</footer>

<script type="text/javascript">
	var visible = false;
	$(document).ready( function() {
		$('.dropdown-toggle').dropdown();
		$("#fuel").click(function() {
			$("#results").load("/search","test=test");
		     $(function() {
	          var wall = new freewall("#results");
	          wall.fitWidth();
	         });
		});
		var cardio_pressed = false;
		$("#cardio").click(function() {
			cardio_pressed = !cardio_pressed;
            if (cardio_pressed)
            {
              $('#cardio').attr('src', '/assets/cardio_pressed.png');
            }
            else
            {
              $('#cardio').attr('src', '/assets/cardio_unpressed.png');
            }
		});
		var strength_pressed = false;
		$("#strength").click(function() {
			strength_pressed = !strength_pressed;
            if (strength_pressed)
            {
              $('#strength').attr('src', '/assets/strength_pressed.png');
            }
            else
            {
              $('#strength').attr('src', '/assets/strength_unpressed.png');
            }
		});
		strength_cardio_pressed = false;
		$("#strength_cardio").click(function() {
			strength_cardio_pressed = !strength_cardio_pressed;
            if (strength_cardio_pressed)
            {
              $('#strength_cardio').attr('src', '/assets/strength_cardio_pressed.png');
            }
            else
            {
              $('#strength_cardio').attr('src', '/assets/strength_cardio_unpressed.png');
            }
		});
		low_pressed = false;
		$("#low").click(function() {
			low_pressed = !low_pressed;
            if (low_pressed)
            {
              $('#low').attr('src', '/assets/low_pressed.png');
            }
            else
            {
              $('#low').attr('src', '/assets/low_unpressed.png');
            }
		});
		low_pressed = false;
		$("#medium").click(function() {
			low_pressed = !low_pressed;
            if (low_pressed)
            {
              $('#medium').attr('src', '/assets/medium_pressed.png');
            }
            else
            {
              $('#medium').attr('src', '/assets/medium_unpressed.png');
            }
		});
		high_pressed = false;
		$("#high").click(function() {
			high_pressed = !high_pressed;
            if (high_pressed)
            {
              $('#high').attr('src', '/assets/high_pressed.png');
            }
            else
            {
              $('#high').attr('src', '/assets/high_unpressed.png');
            }
		});
	});
</script>
