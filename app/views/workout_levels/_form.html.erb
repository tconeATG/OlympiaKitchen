<%= form_for(@workout_level) do |f| %>
  <% if @workout_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout_level.errors.count, "error") %> prohibited this workout_level from being saved:</h2>

      <ul>
      <% @workout_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
